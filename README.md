# ppe_numerique
Application de gestion des dossiers numériques de PPE

* Create a project directory (e.g. ppe_numerique)
* intialise git
* clone the project from `https://github.com/sitn/ppe_numerique`
* create a venv inside the project folder
cd path/to/your/project
python -m venv .venv
* change into the project folder and activate the venv
* install requirements pip install -r requirements.txt
* configure the settings.py with your parameters
* start the server python manage.py runserver
